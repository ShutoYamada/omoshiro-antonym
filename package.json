{
  "name": "omoshiro-antonym",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "api": "pnpm -F 'api'",
    "web": "pnpm -F 'web'",
    "depthcheck": "depcheck"
  },
  "devDependencies": {
    "depcheck": "^1.4.7",
    "prettier": "^3.0.0",
    "turbo": "^1.10.0",
    "typescript": "^5.0.0"
  },
  "workspaces": [
    "services/*"
  ]
}